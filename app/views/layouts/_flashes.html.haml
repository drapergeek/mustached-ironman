-flash.each do |name, msg|
  -if name == :notice
    .alert.alert-success
      %a.close(data-dismiss="alert")x
      =msg
  -elsif name == :alert
    .alert.alert-error
      %a.close(data-dismiss="alert")x
      =msg
  -else
    .alert.alert-info
      %a.close(data-dismiss="alert")x
      =msg
